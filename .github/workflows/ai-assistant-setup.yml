# .github/workflows/ai-assistant-setup.yml
# TODO: this workflow is currently disabled. Enable it once the AI assistant setup is finalized.
# name: AI Assistant Setup Validation
# on: [push, pull_request]
# jobs:
#   validate-config:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - name: Validate AI Assistant Config
#         run: |
#           test -d .clinerules || test -d .config
#           test -f .gitignore
#           grep -q "memory-bank" .gitignore
